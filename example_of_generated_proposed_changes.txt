For device `sandbox-iosxr-1.cisco.com`:
    - Deployed commands:
    - `configure terminal`
    - `interface Loopback0`
    - `description Configured via NETCONF -XR1`
    - `exit`
    - `interface Loopback12`
    - `description Configured via NETCONF -XR2`
    - `exit`
    - `commit`
    - `end`
    - Error encountered: `write memory` command is not supported on this device.

    For device `devnetsandboxiosxe.cisco.com`:
    - Deployed commands:
    - `configure terminal`
    - `interface GigabitEthernet2`
    - `description Network Interface -XE1`
    - `exit`
    - `interface GigabitEthernet3`
    - `description Network Interface -XE2`
    - `exit`
    - `end`
    - `write memory`
    - No errors encountered.


    The deployment of configuration changes was partially successful. Here are the details:

    For device `sandbox-iosxr-1.cisco.com`:
    - Deployed commands:
    - `configure terminal`
    - `interface Loopback0`
    - `description Configured via NETCONF -XR1`
    - `exit`
    - `interface Loopback12`
    - `description Configured via NETCONF -XR2`
    - `exit`
    - `commit`
    - `end`
    - Error encountered: `write memory` command is not supported on this device.

    For device `devnetsandboxiosxe.cisco.com`:
    - Deployed commands:
    - `configure terminal`
    - `interface GigabitEthernet2`
    - `description Network Interface -XE1`
    - `exit`
    - `interface GigabitEthernet3`
    - `description Network Interface -XE2`
    - `exit`
    - `end`
    - `write memory`
    - No errors encountered.